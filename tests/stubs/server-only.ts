/** Stub for Next.js' `server-only` marker package, which has no runnable Node
 *  entry point. Aliased in vitest.config.ts so server modules can be imported
 *  by unit tests without pulling in the Next build pipeline. */
export {};
